# Archive

This directory contains the original development files supplied for the project.
They are retained for traceability and are not the maintained API.

The maintained implementation is under `src/`.
